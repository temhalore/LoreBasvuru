import { BaseModel } from './base.model';

/**
 * EidDTO - Backend ile birebir eşleşir
 * Sadece eid kullanımı için (BaseModel'den extends)
 * Backend: public class EidDTO : BaseDTO { }
 */
export class EidModel extends BaseModel {
  // Boş - BaseModel'den eid kalıtım alıyor
}
